# Verified with Python 3.11. PyTorch는 README의 별도 명령으로 설치.
torch>=2.0
torchvision>=0.15
timm>=0.9

# Numeric
numpy>=1.24
scipy>=1.10

# Computer vision / geometry
opencv-python>=4.5
pyquaternion>=0.9
shapely>=2.0

# Visualization / IO
matplotlib>=3.5
imageio>=2.30
# ffmpeg plugin — tools/predict_video.py가 mp4로 인코딩하는 데 필요.
# 별도 시스템 ffmpeg 없이 동작.
imageio-ffmpeg>=0.4

# Experiment tracking
wandb>=0.15

# Dataset (별도 레포 editable install도 가능 — README 참조)
truckscenes-devkit>=1.1
